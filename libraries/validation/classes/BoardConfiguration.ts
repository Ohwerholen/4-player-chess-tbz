import type { IPiece } from "../interfaces/IPiece";

export class BoardConfiguration {

  private applyCyanDefault(tiles: (IPiece | undefined)[]) {
    // TODO
  }

  public getDefaultConfiguration() {
    const tiles = new Array(12 * 12);
  }
}
